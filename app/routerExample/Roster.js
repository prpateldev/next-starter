import React from 'react';
import { browserHistory } from 'react-router';
export default (props) => (
    <div>
        {/*<p>User ID: {props.params.id}</p>*/}
        <h5>Router App Roster Component</h5>
    </div>
);
